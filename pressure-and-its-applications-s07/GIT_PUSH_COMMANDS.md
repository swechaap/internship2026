# Git Push Commands

Use these commands after creating one GitHub repository named `pressureverse`.

```bash
git init
git add .
git commit -m "Initial PressureVerse project"
git branch -M main
git remote add origin https://github.com/YOUR_USERNAME/pressureverse.git
git push -u origin main
```

After pushing, connect the same repository to Vercel for automatic deployment.
